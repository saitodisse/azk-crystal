```js
// all bellow commands are executed from root folder

// install
(cd share && npm install)

// watch
(cd share && webpack -w)

// lint
./share/node_modules/.bin/eslint share/src

// crystal
crystal run src/azk.cr

// node
node share/dist/main.js
```
